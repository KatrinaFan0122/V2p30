
  # AI Dialogue Interface Prototype

  This is a code bundle for AI Dialogue Interface Prototype. The original project is available at https://www.figma.com/design/SU34ZG5HG0AkleFToH4aW5/AI-Dialogue-Interface-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  